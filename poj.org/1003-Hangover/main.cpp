#include<cstdio>
int main(){
	float c;
	while(scanf("%f",&c),c!=0.00){
		float	total_length=0.0;
		int	cards_count=1;
		while(total_length+=1.0/(++cards_count),total_length<c);
		printf("%d card(s)\n",--cards_count);
	}
	return 0;
}


